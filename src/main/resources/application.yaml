server:
  port: 8888

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/biometric_service_postgres
    username: postgres
    password: postgres

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    open-in-view: true
    show-sql: false
    properties:
      hibernate:
        ddl-auto: update
        format_sql: true
#    database-platform: org.hibernate.dialect.PostgreSQLDialect

twilio:
  account-sid: ACd3bad4d832a2c3a4e5f6g7h8i9j0k1l2m
  auth-token: abc0d1ef23gh456ij789klm
  phone-number: +14155238999

  liquibase:
    enabled: true